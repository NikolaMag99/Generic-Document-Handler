package menu;

import java.awt.Dimension;

import javax.swing.JMenuItem;

import actions.ActionManager;

public class MenuAbout extends JMenuItem{

	public MenuAbout() {
		setText("About");
		setMaximumSize(new Dimension(5, 100));
		addActionListener(ActionManager.getInstance().getAboutListener());
	}
}
